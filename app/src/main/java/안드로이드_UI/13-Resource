리소스(res)
- drawable
    - 이미지
        - 실제 이미지, xml
        - 같은 이미지를 해상도 별로 여러 개 설정 가능
            - 안드로이드가 자동으로 기종에 적합한 해상도를 설정.
- layout
    - 화면을 구성하는 xml
- mipmap
    - 앱 아이콘
- values : 반복적으로 사용하는 문자열이나 색 설정
    - colors
        - 색
            - RGB(red, green, blue)
            - 각각의 색을 2자리 16진수로 표현
            - 맨 앞 2자리는 투명도.
    - string
        - 문자열
    - themes