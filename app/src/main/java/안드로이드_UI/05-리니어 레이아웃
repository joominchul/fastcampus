리니어 레이아웃(Linear Layout)
- 부모가 될 수 있는 뷰(컨테이너 뷰)
- 자식의 위치를 설정
    - 가로, 세로 설정 가능
- 속성
    - orientation : 자식 뷰 방향을 설정
    - weight : 자식이 차지할 크기의 비율을 설정

크기 설정 특수 키워드
- match parent
    - 부모 뷰의 크기만큼 설정
- wrap content
    - 내용물의 크기만큼 설정