범위(scope)
- {} 안 내용.
- 함수, 클래스, 변수(변수 자체가 범위), 패키지
- 규칙
    - 같은 범위 안에서는 공유 가능
    - 하위 범위에서 상위 범위 멤버를 사용 및 재정의 할 수 있다.
        - 상위 범위에서는 하위 범위 멤버를 사용 및 재정의 할 수 없음.
    - 범위에 따라 접근 가능한 범위가 있고, 접근 불가능한 범위가 있음.

접근제한자
- 내가 어덯게 접근을 제한하는지에 따라 달라짐.
- public(기본), internal, private, protected
- 변수, 함수, 클래스 선언 시 맨 앞에 적어준다.
    - private val num:Int = 10
- 패키지
    - public : 어디에서든 접근 가능
    - internal : 같은 모듈 안에서 접근 가능
    - private : 같은 파일 안에서 접근 가능
    - protected : 사용 안 함
- 클래스
    - public : 클래스 외부에서 접근 가능
    - internal : 사용 안 함
    - private : 클래스 안에서만 접근 가능
    - protected : 클래스 자신과 상속 받은 클래스에서 접근 가능

물리적 구성
    프로젝트
    - 모듈들로 구성된다.
        모듈
            - 라이브러리로 구성된다.
            라이브러리
                - 파일, 폴더.
                - 특정 기능.

패키지
- 논리적 단위
- 파일 최상단에 패키지명 적어둠.