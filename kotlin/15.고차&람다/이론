고차함수 : 함수를 인자로 받고 결과 값으로 내보낼 수 있다.
- 함수 타입 표시 방법
    fun function1(함수명 : (인자 함수의 파라미터 타입,...) -> 인자 함수의 리턴 타입)
- 고차함수 호출 방법
    function1(::함수명)
    람다를 호출 시 :: 사용 안 함.
    간단한 경우 바로 만들어 사용 가능
        - function1 {a, b -> a+b}

람다
- 그 자체로 고차함수이기 때문에 별도의 연산자 없이 변수에 담을 수 있다. 내용의 마지막 줄이 리턴된다.
풀버전
    val lam : (파라미터 타입,...) -> 리턴 타입 = {파라미터 이름:파라미터 타입,... -> 내용}
생략버전
    - 중괄호 안 파라미터 타입 생략 가능
    val lam : (파라미터 타입,...) -> 리턴 타입 = {파라미터 이름,... -> 내용}
    - 람다 타입 생략 가능
    val lam = {파라미터 이름:파라미터 타입,... -> 내용}
파라미터가 없는 버전
    val lam : () -> 리턴 타입 = {내용}
- 파라미터가 1개인 경우 it을 사용
    - val lam : (파라미터 타입,...) -> 리턴 타입 = { println(it)} -> forEach와 비슷
