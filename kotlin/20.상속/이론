상속(inheritance)
- 확장된 클래스를 만드고 싶을 때 사용.
- 이미 존재하는 클래스를 합칠 때 사용.
- 여러번 가능
- 다중 상속 불가능. -> 여러 개 클래스 상속 불가능.
- 상속으로 인해 유지보수가 쉬워짐.
- 키워드
    - open 키워드로 설정된 클래스만 상속할 수 있음.
        - open class 클래스명
    - final 키워드로 설정된 클래스는 상속할 수 없음(기본).
        - final class 클래스명
        - class 클래스명
    - super 키워드 : 슈퍼/부모 클래스를 의미.
- 슈퍼클래스/서브클래스, 부모클래스/자식클래스
- 서브클래스는 슈퍼클래스의 생성을 책임져야 한다.
- 서브클래스는 슈퍼클래스에서 사용하고 있는 멤버와 동일한 이름의 멤버를 만들 수 없다.
- 자식클래스는 부모클래스가 가지고 있는 모든 걸 상속받는다.

오버라이딩(overriding)
- 오버라이딩을 통해 슈퍼클래스가 가지고 있는 함수를 재정의 할 수 있음.
    - 재정의 : 재선언. 이전 정의를 번복하고 새롭게 정의. 이전 것을 덮어씀.
    - open fun 재정의할 함수명()
    - override fun 재정의한 함수명()

오버로딩(overloading)
- 동일한 함수를 여러 개 만드는 것은 불가능. 리턴 타입이 달라도 불가능.
    -> 함수의 인자를 다르게 하면 가능.