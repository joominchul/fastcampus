형변환(Type Casting)
- 타입 : 자료형, 클래스
    - 보통 to자료형() 함수를 사용.
- 캐스팅 : 다른 자료형을 변환. 무조건 가능한 건 아님.
- is : 형변환 가능한지 확인 키워드. true, false 리턴
    - 클래스명1 is 클래스명2
    - 스마트 캐스팅
        - is 키워드를 사용한 if문 안에서는 코틀린이 자동으로 캐스팅을 해준다.
        - val 변수명:부모 클래스 = 자식 클래스() -> 스마트 캐스팅 사용 시 val만 가능.
        변수가 부모 클래스 멤버만 사용 가능하고, 자식 클래스 멤버는 사용 불가능.
        if (변수명 is 자식 클래스){
            이 안에서는 변수가 자식 클래스 멤버 사용 가능.
        }
- as : 지정한 타입으로 캐스팅을 시도하고, 불가능한 경우 예외 발생.
    - var 변수명: 지정 타입 클래스 = 다른 변수 as 지정 타입 클래스
    -> 다른 변수를 지정 타입 클래스로 캐스팅을 시도하고, 불가능하면 예외(ClassCastException) 발생.